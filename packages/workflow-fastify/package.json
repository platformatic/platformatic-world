{
  "name": "@platformatic/workflow-fastify",
  "version": "0.8.1",
  "description": "Fastify plugin that mounts Vercel Workflow SDK handlers, backed by @platformatic/world",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "test": "node --test test/*.test.ts",
    "lint": "eslint"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "dependencies": {
    "@platformatic/world": "workspace:*",
    "fastify-plugin": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "fastify": "^5.3.3",
    "typescript": "^6.0.0"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/platformatic/platformatic-world.git",
    "directory": "packages/workflow-fastify"
  },
  "license": "Apache-2.0"
}
